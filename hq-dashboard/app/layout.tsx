import './globals.css';
export const metadata = { title: 'POLARIS HQ — NCPOR Command', description: 'HQ SOC Enterprise Dashboard' };
export default function Layout({ children }: { children: React.ReactNode }) {
  return <html lang="en"><body>{children}</body></html>;
}
